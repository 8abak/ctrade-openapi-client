{
  "version": "20260425_sql_export_layout_cleanup",
  "description": "Current deploy steps for the SQL export backend/frontend cleanup. Git sync is handled before this runner starts; restart datavis.service, then hard refresh the browser to pick up the updated SQL assets. No SQL migration, backfill, or scenario rerun is required.",
  "actions": [
    {
      "id": "restart_datavis_service",
      "name": "Restart datavis.service",
      "description": "Restart the FastAPI service so the SQL export and layout update is live.",
      "type": "restart_service",
      "service": "datavis.service",
      "timeout_seconds": 60
    }
  ]
}
