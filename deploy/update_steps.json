{
  "version": "20260425_motion_model_scenarios",
  "description": "Run the journaled motion research update runner so SQL, backfill, validation, and the datavis restart share one auditable command path.",
  "actions": [
    {
      "id": "apply_motion_trade_spots_update",
      "description": "Run the motion trade spots update runner with journaling, DB validation, backfill, datavis restart, and API verification.",
      "type": "run_command",
      "command": "bash deploy/scripts/apply-update-steps.sh",
      "required": true,
      "safe_to_rerun": true,
      "timeout_seconds": 5400
    }
  ]
}
